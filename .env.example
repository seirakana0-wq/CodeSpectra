# LLM provider (OpenAI-compatible)
LLM_API_KEY=sk-xxx
LLM_BASE_URL=https://token-plan-sgp.xiaomimimo.com/v1
LLM_MODEL=mimo-v2.5-pro

# Optional: GitHub token to bump rate limit from 60 -> 5000/h
# (no oauth, single token used by the server only)
GITHUB_TOKEN=
